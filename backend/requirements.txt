fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
sqlalchemy>=2.0.35
psycopg2-binary>=2.9.9
python-multipart>=0.0.12
pytesseract>=0.3.10
jellyfish>=1.0.0
opencv-python-headless>=4.10.0,<5
pillow>=10.4.0
numpy>=1.26.0
torch>=2.4.0
torchvision>=0.19.0
scipy>=1.14.0
scikit-learn>=1.5.0
pytest>=8.3.0
httpx>=0.27.0
python-dotenv>=1.0.1
requests>=2.32.0
tqdm>=4.66.0
web3>=7.0.0

# facenet-pytorch is installed separately with --no-deps (see Dockerfile / README):
# its declared pins (torch<2.3, torchvision<0.18, numpy<2, Pillow<10.3) are years
# stale and conflict with the modern versions above, even though its actual code
# runs fine against them -- verified empirically, see scripts/calibrate_face_threshold.py.
# requests/tqdm above are facenet-pytorch's own lightweight deps, installed
# normally since --no-deps skips them too.
